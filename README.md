# ML-PROJECT
This Flask web app deploys a pre-trained ML model (.pkl) for Boston Housing Price Prediction. It uses Python and NumPy. The app accepts 13 features via a POST request to the /predict route, calculates the price, and displays the result. I am currently working on this project.
